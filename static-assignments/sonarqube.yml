---
- hosts: sonarqube
  tasks:
      - name: Install package dependencies.
        package:
          name: "{{ item }}"
          state: "present"
        with_items:
          - unzip
  roles:
    - /home/ec2-user/ansible-config-mgt/static-assignments/sonarqube.yml
