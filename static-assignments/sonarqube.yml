---
- hosts: sonarqube
    pre_tasks:
      - name: Install package dependencies.
        package:
          name: "{{ item }}"
          state: "present"
        with_items:
          - unzip
    roles:
      - /home/ec2-user/ansible-config-mgt/roles/sonarqube
